#include <stdio.h>
/**
 * main - Print alphabet in reverse order.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char c;
char a;
for (c = 'z'; c >= a; c--)
{
putchar(c);
}
putchar('\n');
return (0);
}
